/**
 * @deprecated Prefer importing from `./hermescloud`. This compatibility shim
 * will be removed after the Hermes Cloud slug fully replaces the legacy
 * `lobehub` identifier across downstream extensions (target 2025-06-30).
 */
export { default, planCardModels } from './hermescloud';
